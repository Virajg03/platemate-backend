spring.application.name=PlateMate


# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/platemate
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# Razorpay Configuration
# Uncomment and set your actual test credentials:
#rzp_test_RkT4jplwd1NaBg Test key id
#mn61liAFF1aFQteSIXJeMrsX Test Key secret

payments.razorpay.keyId=rzp_test_RkT4jplwd1NaBg
payments.razorpay.keySecret=mn61liAFF1aFQteSIXJeMrsX
# Webhook Secret: Create a webhook in Razorpay Dashboard (Settings > Webhooks)
# Enter any secret string when creating the webhook, that becomes your webhook secret
# For local testing, you can use a placeholder like "test_webhook_secret_12345"
payments.razorpay.webhookSecret=test_webhook_secret_12345
payments.razorpay.currency=INR

# payments.razorpayx.keyId=${RAZORPAYX_KEY_ID:rx_test_your_key_id}
# payments.razorpayx.keySecret=${RAZORPAYX_KEY_SECRET:rx_test_your_key_secret}
# payments.razorpayx.webhookSecret=${RAZORPAYX_WEBHOOK_SECRET:whsecx_test_webhook_secret}

spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false


#file related all configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=yourSuperSecretKeyThatIsAtLeast32Characters
jwt.token.expiration=86400000
jwt.refresh.token.expiration=604800000

# Password Encoder
spring.security.user.password=custompassword

# Email Configuration
# Gmail SMTP Configuration
# IMPORTANT: Make sure you've enabled 2-Step Verification and generated an App Password
# Go to: Google Account -> Security -> 2-Step Verification -> App passwords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=aakashpavar87@gmail.com
# App Password: Generate from Google Account Settings (16 characters, no spaces)
# IMPORTANT: Make sure 2-Step Verification is enabled first!
# The password should be exactly 16 characters with NO spaces
# Original: "xgij wtao iqxh pdjc" → Should be: "xgijwtaoiqxhpdjc" (verify this matches your Google App Password)
spring.mail.password=xgijwtaoiqxhpdjc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Alternative: Try SSL on port 465 if port 587 doesn't work
# Uncomment these lines and comment out the port 587 settings above:
# spring.mail.port=465
# spring.mail.properties.mail.smtp.ssl.enable=true
# spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email Service Configuration
email.from.name=PlateMate
email.from.address=aakashpavar87@gmail.com